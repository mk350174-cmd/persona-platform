# persona-platform — merged requirements
# Union of Persona (persona_math/needle/persona_mcp/validation/experiments) +
# the platform api/ deps (from personaplatform.zip). Deduplicated by package.
# Heavy ML deps (torch/transformers/peft/bitsandbytes/gguf) install on Colab/GPU.

alembic>=1.13.0
anthropic>=0.30
bitsandbytes     # INT4 / 8-bit quantization kernels
fastapi>=0.110.0
gguf            # INT4 / GGUF export (quantizer.py)
# Persona packages (persona_math/needle/persona_mcp) are vendored locally in this
# repo — no external git dependency needed.
google-generativeai>=0.7
# gradio>=5.0.0  # HuggingFace Spaces demo only — see spaces/persona-hub/app.py
groq>=0.9
httpx              # Ollama (Llama teacher)
jinja2
jsonlines
# mcp>=1.0  # persona_mcp only — not needed by API tests
networkx>=3.0
numpy>=2.0.0
openai>=1.0
peft             # LoRA (drop-in alternative to the built-in LoRALinear)
pydantic[email]>=2.0.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
pytest-xdist>=3.5.0
python-dateutil
python-multipart>=0.0.9
pyyaml>=6.0
scikit-learn>=1.2.0
scipy>=1.14.0
argon2-cffi>=23.1.0
resend>=2.0.0
redis>=5.0.0
sentry-sdk[fastapi]>=1.40.0
posthog>=3.0.0
supabase>=2.4.0
slowapi>=0.1.9
sqlalchemy>=2.0.0
stripe>=8.0.0
tiktoken>=0.7.0
tokenizers
torch>=2.0
tqdm
transformers>=4.40
uvicorn[standard]>=0.29.0
websockets>=12.0
